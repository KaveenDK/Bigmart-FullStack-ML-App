# Model folder

Put these files here once Ahasna has trained and exported them:

- `model.joblib`
- `scaler.joblib`
- `encoder.joblib` (if one was used)

`app.py` loads its model files from this folder — see the TODOs at the top
of `backend/app.py`.
